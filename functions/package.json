{
  "name": "myapp-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for myapp",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.1.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/node": "^20.5.0",
    "typescript": "^5.1.0"
  }
}
